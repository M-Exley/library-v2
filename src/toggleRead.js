/* this function will toggle the read status when the selection is clicked on the dropdown list.
    the function should be used in the global scope - index.js.
    -- Modified - the function is going to run inside options().
    options() is called when pushBooktoCont() is called     
    
    this is no longer being used but I will leave it here for future use      */

// export default function toggleRead(string) {
//   const resultOfClick =
//     String(string) === "Mark as not read" // works
//       ? true
//       : false;
//   return resultOfClick;
// }
